{
  "name": "prism-stellar-earn",
  "version": "0.1.0",
  "private": true,
  "description": "Public Stellar integration workspace for Prism Earn: wallet onboarding, Stellar settlement, Etherfuse stablebond markets, and NEAR Chain Signatures validation.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@prism/demo-earn",
    "build": "npm run build --workspace=@prism/demo-earn",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
